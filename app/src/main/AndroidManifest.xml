<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.materialdesign">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/IndigoTheme">
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.viewpager.ApiActivity" />
        <activity android:name=".view.transitions.AnimationsActivity" />
        <activity android:name=".view.transitions.TransitionActivity" />
        <activity android:name=".view.transitions.ExplodeActivity" />
        <activity android:name=".view.transitions.TransformActivity" />
        <activity android:name=".view.transitions.PathMotionActivity" />
        <activity android:name=".view.transitions.AnimationsFabActivity" />




    </application>

</manifest>